# Session Handoff Archive — Car Brite — 2026-08-31

> Archived per `session-handoff-protocol.md` / HANDOFF.md's own 200-line cap rule. This is the full narrative history through project.json version 19. Current HANDOFF.md carries only the latest state forward from here.

---

## Full "Where we left off" history (chronological)

Project formally started this session. Ran orchestrator Step 0 preflight (`check-env.sh --platform headless`): Node.js v20.18.0 FAILs the v22+ requirement — overridden (logged in audit_log) to let G0 document work proceed, but flagged as a hard blocker before G3 scaffold. Created `project.json` at project root (id `864761f5-3fb1-4721-9c43-6632fc4b19cf`), validated against `_contracts/project-json.schema.json` (passes). PM Agent Step 0 ingested `sow-spec.md`: all validation checks passed except `validation.status` being `passed_with_logged_deviations` rather than a literal `passed` (soft-fail, not blocking — 5 rewrites all have sales-confirmed audit trail). Computed SOW completeness score = 83/100 (weighted formula, clears the 60 threshold) — but the separate hard G0 gate (`13-g0-intake-gate.md`) needs its own 8-question abbreviated intake answered before G0 can close. Batched those 8 questions and posted them.

GitHub repo URL received. Ran G1 milestone/sprint decomposition (5 milestones, 12 sprints) — pattern-based estimate came back 5.4-9.7x the signed 45h (242-438h vs 45h). Presented the HALT/renegotiation surface. **Internal PM (Ajay Sir): Option D, ACCEPT TIGHT — CONFIRM.** G1 passed, stage advanced to `design`, architecture A confirmed. Sprint hours then rebased to the exact 45h SOW breakdown.

Then a correction came in: the 45h figure itself was a mistake — 54h is correct (the previously-dismissed "Hours Bifurcation" table). Updated BOTH `sow-spec.md` (total_hours→54, cost→$2,160, Rewrite 6 added, Rewrite 3 marked superseded not deleted, Module 6 "Unallocated buffer" row added) and `project.json`. The 9h gap was intentionally NOT allocated at first — user chose to specify line items later rather than have Claude guess.

Both open items resolved next: (1) the 9h buffer's purpose defined as a client feedback buffer, not unallocated. (2) G2 mockup decision made: reference-only, after actually opening both HTML files locally in a browser to verify (13.4MB/8.3MB self-extracting bundler exports — confirmed they render, found a real mobile nav bug in the reference itself, flagged not silently ignored).

G2 gate opened (not yet passed) — scope-approach decided, but formal Design lead + Client sign-off still needed. Dev team then reported S2.1 environment setup already done; set to `qa` not `done` (self-report ≠ QA confirmation). User said "Start G2" → formal gate block presented → **G2 CONFIRMED by Ajay Sir**, design frozen, stage advanced to `development`.

S2.1 was then actually built, not just claimed — verification found the self-report was wrong (no scaffold existed, Node still v20). Real Hydrogen scaffold created via `@shopify/cli-hydrogen` (first attempt's `--force` flag nearly `rmdir`'d the whole repo — caught by a Windows file lock, redone safely via an isolated temp dir + manual merge). Fixed a real `.gitignore` gap (was empty/misnamed). Root-caused a real build failure (rolldown needs Node ≥22.12.0, workstation had exactly 22.0.0) — clean reinstall fixed it. S2.1 → `done`.

Dev server then confirmed connected to the wrong store ("wds55", generic Shopify demo data) — halted before building anything against it, asked in plain language. **Confirmed: wds55 is an intentional WebDesk dev/test store** — production store swap to the real Car Brite store still required before G6 (tracked in `sow.gaps` as `production_store_swap`).

S2.2 (Metaobject-driven homepage) built end-to-end: user added `ADMIN_API_TOKEN` to `.env`; verified the exact Admin API mutation shape via Shopify's docs before calling it; created the `homepage_content` Metaobject definition + `main-homepage` entry via credential-blind scripts (`.env` values never read directly, only key names); caught that Storefront access defaulted to `NONE` and fixed to `PUBLIC_READ`; rebuilt `_index.jsx` + `app.css` to match the G2-approved reference; verified in a real browser (desktop clean, mobile 375px responsive, nav collapses correctly unlike the reference HTML's own bug).

S3.1 (Collection page): user chose "build everything in the reference" + keep the non-Shopify "Buy on Amazon" link as a static placeholder — logged as scope expansion (risk R7). Read the reference's live DOM (not a screenshot). Built both `collections.$handle.jsx` and `collections.all.jsx` (different query shapes). Hit and fixed a real 500 error (`products` query has no `filters` arg, only `query` search-syntax) plus a `CREATED`/`CREATED_AT` mismatch and a nonexistent `product.badge` field — all caught before/during verification, not shipped broken.

S3.2 (PDP) built and verified — no design-contract doc exists for it, styled consistently with brand tokens instead. Added inventory status, deferred related products, conditional Recharge-style subscription block (only renders when real selling plans exist).

Then: user said the build didn't match the reference mockups. First pass had only checked interactive HTML, never the static pixel mockups (`Pearls_HP.jpg`, `Pearls_CP.jpg`). Found and fixed: "The Routine" block is a Shop-nav mega-menu, not a section (wrongly built as one on the collection page — removed); homepage was missing 6 of 7 real sections (rebuilt: pod system, brand/stats, subscription, testimonials, FAQ, newsletter); footer was still bare scaffold default (rebuilt full PEARLS footer, site-wide); a real accessibility bug where `body` had no explicit `color-scheme`, making dark text unreadable under a dark browser preference on several sections (fixed globally); collection page Amazon button/photos/layout details fixed. PDP reference absence reconfirmed by re-listing `Design/`.

Then a second "design is not proper" pass: pulled exact `getComputedStyle()` values from the reference's live DOM instead of guessing. Fixed: header was scaffold-default white (reference is translucent navy) — global fix; hero gradient was an oversimplified 2-stop approximation, replaced with the real 3-layer gradient; hero had no photo at all, added (temporary static asset — `hero_image` Metaobject field is null because the Admin API token lacks `write_files` scope, `scripts/upload-hero-image.mjs` exists but is blocked, logged as a risk); body font (Manrope) was never wired up; Manrope then silently failed to load — root cause was Hydrogen's default CSP blocking `fonts.googleapis.com` with no visible console error, found via network-request inspection, fixed in `entry.server.jsx`; button radius was 4px vs the reference's 999px pill; H1 was capped at 72px vs the reference's 96px.

Then the user sent a formal, detailed re-review brief insisting mockup-fidelity (not dynamic data alone) is the acceptance bar, with an explicit gap-analysis-table + 8-section final-report format requested. Found one more major, previously-undetected gap: **Header.jsx was still 100% generic scaffold** — plain shop.name text instead of the "PEARLS" logo, nav pulled from wds55's own Shopify menu ("Home/Catalog/Contact") instead of the mockup's "Shop/How it Works/About Us/Contact Us" with icon-based search/cart. Fixed globally (real logo, static nav labels matching the pattern already used in Footer.jsx, real search/bag SVG icons from the provided assets). Verified: Add to Cart tested for real (cart drawer opens, badge updates), mobile responsive checked across hero/pod-system/subscription/collection grid — all correct. Also found and fixed a real bug in 5 of the project's own setup scripts: CRLF line endings in `.env` (Windows-authored) broke a hand-written regex parser (JS `.` excludes `\r`), silently failing to read 8 of 9 keys — production app unaffected (uses a proper env loader), tooling-only bug, fixed. Used a corrected read-only Admin API script to definitively verify the Metaobject definition + entry are correctly configured and match live-rendered content exactly.

---

## Full risk register context (see project.json for live status)

R1–R10 recorded across this session, spanning: bundled zero-hour items (mitigated — client feedback buffer), Hours Bifurcation discrepancy (mitigated — corrected to 54h), Node version (mitigated — v22 confirmed), underquote vs pattern estimate (accept-tight, Ajay Sir), wrong dev store (mitigated — wds55 confirmed intentional), S3.1 scope expansion (open, watch hours), first-pass mockup-fidelity gaps (mitigated), header scaffold-default gap (mitigated), CRLF script parsing bug (mitigated). Full detail in `project.json.risks[]` and `.audit_log[]`.

---

## Superseded decision text (kept for audit trail — see sow-spec.md Rewrite 3/6 for the canonical version)

Node v22+ environment FAIL was overridden to let G0 intake (non-code) proceed; `project.json.project.platform` set to `shopify` / `project_type` to `new-development` for schema compliance (schema has no `headless`/`new-build` enum values, actual engagement recorded in `_headless_arm_notes`).
