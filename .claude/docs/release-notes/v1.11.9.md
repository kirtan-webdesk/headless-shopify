# WebDesk AI Delivery System — v1.11.9

**Release date:** 2026-07-08
**Type:** PATCH — Customization Register ratified into spine (D-CUST-RECON-01)
**Trigger:** Shopify skill-dev window HANDOFF 2026-07-02

---

## Why v1.11.9

Shopify window escalated a new cross-cutting capability: a **customization register** for existing-site projects (migration / redesign / version-upgrade). Belongs in spine, master-owned, referenced by every platform arm's redesign/migration/version-upgrade project skills. No parallel per-platform reinvention.

---

## What shipped

### 1. `_spine/shared-knowledge/customization-register.md` — new spine file (tier 2)

Adopted from Shopify window's proposal. Two things I changed before shipping:

- **Dropped "proposed" banner** → replaced with "ratified as D-CUST-RECON-01" marker
- **Genericized platform-specific file references** per v1.11.6+ spine-purity policy (D-EDITION-FILTER-01 rule 2)
  - Before: explicit `shopify/projects/version-upgrade/knowledge/10-live-parity-audit.md` and `shopify/projects/redesign/knowledge/05-functionality-preservation.md` paths
  - After: `<active-platform>/projects/version-upgrade/knowledge/NN-live-parity-audit.md` pattern, pointing to each arm's own 00-overview.md for the concrete file names

Otherwise identical to their proposal.

### 2. D-CUST-RECON-01 ratified in canonical inventory

Added under D-SHIP-GATE-01 (spine-level universal codes group). Full text per HANDOFF wording. Filters into every edition (Shopify + WP + BC + Headless all need it — every platform has redesign / migration / version-upgrade project types).

---

## What was NOT shipped (deferred to next Shopify HANDOFF)

Per Shopify HANDOFF §"What was done in-window": their local interim bundle also contains 4 arm-side changes that reference the new spine file:

- `shopify/projects/redesign/knowledge/05-functionality-preservation.md` (added Method 7 + G1 reconciliation)
- `shopify/projects/version-upgrade/knowledge/10-live-parity-audit.md` (added reconcile section)
- `shopify/projects/version-upgrade/knowledge/09-upgrade-launch-checklist.md` (added register-reconciliation gate line)
- `shopify/knowledge/changelog.md` (logged E8)

These are NOT in master's canonical yet — they live in the Shopify window's `webdesk-shopify-v1.11.7-custreg.zip` interim bundle only. Next Shopify HANDOFF should upload those 4 file changes for official incorporation. Until then:

- v1.11.9 Shopify edition ships WITHOUT the arm-side changes
- Shopify window continues using their interim bundle for their local work
- The spine reference in their arm files STILL resolves (points to v1.11.9's spine copy)
- Next Shopify HANDOFF folds the 4 arm-side changes into official Shopify edition

---

## Directive for WP window (surface at next WP HANDOFF cycle)

**When the WP window builds Redesign / Migration / Version-Upgrade project skills, they must reference `_spine/shared-knowledge/customization-register.md` and NOT reinvent it as WP-parallel code.**

Same principle applies to BC + Headless windows if they build project skills covering existing-site scenarios.

The register is cross-platform BY DESIGN. Each arm contributes only its **discovery method** (how live-site scans populate the register); the register model, reconciliation rule, and gate enforcement are universal.

WP window's pending project skills: Migration, Version Upgrade, B2B, Multi-region. Two of those (Migration, Version Upgrade) will need to feed the register. Copy this section into their next window prompt or brief when you next open that window.

---

## Master's soft observations (documented, not blocking)

Reviewing the Shopify proposal, I flagged two things to the user before ratifying:

1. **Commercial machinery.** "Discovered, not in SOW → add-to-scope OR client-approved exclusion" is textbook change-order behavior. File notes "per the project's commercial terms" which is fine documentation but leaves operational question open. Not a spine problem — sales-ops question separate from the delivery skill.

2. **G1 timing lag.** Register is populated during discovery (G0.5), reconciled at G1. Findings pile up during the gap on complex sites. Anti-pattern #2 ("Discovering but not deciding") is the enforcement lever; anti-pattern #4 ("Reconciling at signing only") partially addresses. Inherent to the workflow, not a defect.

Neither warranted a rewrite. Ratified as-is with only the two genericization changes noted above.

---

## Cross-window protocol observation

Shopify window's HANDOFF is now the standard for how escalations should look:

- Diff-based verification of scope
- Explicit "here's what I did in-arm; here's what master must do; here's what's deferred"
- Cross-platform propagation note flagged in §"Cross-platform note"
- Verification evidence attached

Third consecutive Shopify HANDOFF at this quality bar. Whoever's driving that window is doing textbook work.

---

## Ship-gate integrity

All 6 bundles built through `ship-bundle.sh` (D-SHIP-GATE-01 enforced):

- All 6 pass `verify-edition-integrity.sh` for their respective editions
- Contamination scan clean across all lean editions
- Genericization of the new spine file verified — 0 platform-arm file paths remain
- Frontmatter validation: 0 errors across all bundles

---

## Phase 2 — sow-builder ID emission (deferred, per Shopify HANDOFF)

Not in v1.11.9. Shopify HANDOFF flags: `sow-builder` `06-spec-output-format.md` + `07-client-doc-format.md` should be updated to emit each customization with a stable ID at both altitudes, so SOW/spec carry IDs the register can match directly. Not required for the register to work (matches on description until IDs exist), but closes the loop.

`sow-builder` is a standalone sales-owned skill. When Phase 2 ships, it'll be a separate deliverable (updated `sow-builder` files, installed by sales team), not a bundle-cycle change.

---

## Version state

| Component | Was | Now |
|-----------|-----|-----|
| System | v1.11.8 | **v1.11.9** |
| `_spine/shared-knowledge/customization-register.md` | (did not exist) | **NEW (tier 2)** |
| Canonical `decision-inventory.md` | +D-CUST-RECON-01 | ratified |
| Shopify arm content | unchanged | unchanged (interim bundle still valid locally) |
| Other platform skills | unchanged | unchanged |

---

## Honest note

8 releases in ~6 days. I noted this after v1.11.7. Cadence hasn't slowed. Not going to keep pointing it out — you're aware. Watching HANDOFF quality as the actual signal.

Today's Shopify escalation was a clean 15-minute master job (ratify + adopt + reissue). Would have been 45 minutes if I hadn't caught the platform-path contamination during pre-flight — that's the ship-gate paying dividends. First contamination-caught-early on a new spine file this session.

---

Generated: 2026-07-08 by master (v1.11.9 — customization register ratified)
