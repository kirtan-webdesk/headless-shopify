# WebDesk AI Delivery System — v1.11.33

**Release date:** 2026-08-27
**Type:** RECOVERY PATCH — folds two Headless-window deliveries dated 2026-08-12 that master failed to fold in v1.11.30, v1.11.31 or v1.11.32. Also carries the ScriptTag-sunset disclosure that was unmasked by the recovery. Headless-only. Zero other-arm content edits.
**Trigger:** `headless-RECOVERY-dropped-work.zip` from Headless window 2026-08-27, catching a three-release fold gap.

---

## Confirmation the fold landed (Headless window's explicit ask)

**Yes.** All 28 files from the recovery zip are folded into v1.11.33 and present in `webdesk-headless-v1.11.33.zip`. Byte-count verified: `find webdesk-headless-v1.11.33/skills/headless -type f | wc -l` = 28, matching the recovery zip file-for-file. `templates/env-preflight.md` and `templates/discovery-audit.md` are both in the bundle. `verified-facts §14/§15/§16` all present. `SKILL.md` at 0.16.0. Content-hash arm-version-bump check passed at ship: `headless — content changed, SKILL.md bumped 0.15.0 → 0.16.0.` If v1.11.33 or any later bundle is missing any of these files, it is a regression against this named baseline — not the same class of drop again.

## What master failed at, and what changed

Two deliveries — `headless-pilot-templates` and `headless-verified-findings`, both 2026-08-12 — were not folded. All three subsequent releases (v1.11.30/31/32) shipped Headless content from the v1.11.29 base. Recovery caught by the Headless window auditing a later bundle, not by master.

**Named class of failure** (Headless window's framing, adopted verbatim into inventory):

> *"delivered work that nobody folds is indistinguishable from work never done. Same class as the K4 findings (a recorded gap that nobody converts into work is indistinguishable from an unrecorded one), at the release-fold boundary instead of the KB-note boundary."*

**Structural gap disclosed, deferred to post-Headless-K4 batch per D-MASTER-SCOPE-01:** the ship-gate content-hash check compares a bundle against the *previous bundle*. Work that never entered a bundle is invisible to it by construction. A candidate fix — a delivery-landing check reconciling a per-arm delivery ledger (or an inbox convention) against the folded diff — is filed for the batch. Not fixed in-release; disclosed and named. The pattern that would have caught this is exactly the shape of D-HL-SPEC-01's observability test one level up: *is there a check that would fail if a delivery went missing?* At the ship-gate boundary, currently, no.

## Cost of the drop, unmasked at fold

`verified-facts §15` records the `checkout.liquid` ScriptTag sunset dates for both plans. Non-Plus sunset was **2026-08-26 — one day before this release.** The shipped arm had no record of this fact through v1.11.30/31/32, when it was verified on the 12th. The KB now records both dates as **in effect**, and reframes the audit question accordingly: not "when does this break" but "what did the client already lose, and did anyone notice?" Nothing errors when a ScriptTag stops firing on the covered pages.

**Cross-arm disclosure (safe under D-MASTER-SCOPE-01 amendment as a disclosure, not a content edit):** this is a Shopify-client-relationships matter, not a headless-arm matter. Naming the owner in inventory rather than only in these notes ensures the record survives past this document being re-read. No Shopify arm content was touched in this release.

## What was in the recovery zip

- **`pointers/verified-facts.md` §14** — checkout UI extensions **narrowly** Plus-only (information/shipping/payment step targets); other targets not stated (absence of a restriction is not a grant); **branding/styling still open**. D-HL-APPS-01 gate item 3 **narrows, does not close**.
- **`pointers/verified-facts.md` §15** — ScriptTag sunset in effect for both plans. See above.
- **`pointers/verified-facts.md` §16** — Hydrogen versioning and bundle-size tooling.
- **`templates/env-preflight.md`** — the pilot-day-one artifact for D-HL-ENV-01 (blocks build start).
- **`templates/discovery-audit.md`** — the pilot-day-one artifact for D-HL-DISCOVERY-01 (blocks pricing).
- Propagated updates to `knowledge/08-app-integrations/00-classification.md`, `knowledge/09-forbidden.md`, `knowledge/11-environment-preflight.md`, `knowledge/12-discovery-audit.md`, and `architectures/shopify-hydrogen-oxygen/00-reference.md`.
- **`SKILL.md`** version 0.15.0 → 0.16.0, description updated.

## 4e disclosure — capability gaps not covered by D-HL-SPEC-01

The Headless window's K4 findings had a fifth item that was not carried into D-HL-SPEC-01's ratification: four capability gaps (Metaobject-driven section patterns, channel publication scoping, navigation sourcing, live-store safety). D-HL-SPEC-01 covers **detection** — `HL-SPEC-003` will stop a static homepage at G4 — not **capability**. A developer still has no Metaobject pattern to build the dynamic one with.

**Blocking is not teaching.** Filed as a distinct work item for the Headless window: **`HL-CAP-*` prefix reserved** in inventory. Content deferred, not scope-broadening D-HL-SPEC-01. Fixing this is a knowledge-authoring pass, not a rule change.

## Ship-checklist per D-MASTER-SCOPE-01

Diff v1.11.32 → v1.11.33:

- `README.md` (version bump)
- `_decisions/decision-inventory.md` (recovery disclosure, ScriptTag cross-arm disclosure, 4e/HL-CAP reservation, D-HL-APPS-01 item 3 narrowing, sales-facing status update)
- `skills/headless/SKILL.md` (0.15.0 → 0.16.0)
- `skills/headless/architectures/shopify-hydrogen-oxygen/00-reference.md`
- `skills/headless/pointers/verified-facts.md` (§14/§15/§16 added)
- `skills/headless/knowledge/08-app-integrations/00-classification.md`
- `skills/headless/knowledge/09-forbidden.md`
- `skills/headless/knowledge/11-environment-preflight.md`
- `skills/headless/knowledge/12-discovery-audit.md`
- `skills/headless/templates/env-preflight.md` (new)
- `skills/headless/templates/discovery-audit.md` (new)

**Zero other-arm content files touched.** Shopify, WP, BC, Magento arms are byte-identical to v1.11.32. Verified by directory diff before ship.

## Ship-gate results

All 6 bundles PASS. Content-hash confirmed `headless — content changed, SKILL.md bumped 0.15.0 → 0.16.0.` `wp-safe-deploy.sh` stripped from Shopify/BC/Headless/spine-only bundles by the packaging step (v1.11.32 gate catch, applied consistently in v1.11.33 — the underlying "strip by allowlist vs by convention" question remains deferred).

---

## For the Headless window

Reinstall `webdesk-headless-v1.11.33.zip`. Fold-landed confirmation above.

The three-release drop is on master, not on you. Both August 12 zips are in the bundle now, verified byte-by-byte against your delivery. The delivery-landing check you named is the structural fix; it lives in the post-Headless-K4 batch alongside the other tooling deferrals. Until that batch ships, the class of failure that produced this recovery is still open — the mitigation is your delivery cadence and my ship-checklist honesty, not tooling.

4e is not lost. `HL-CAP-*` reserved; content deferral is disclosed to the user in this release. Whether to author it before or after the next pilot is your call — the pilot itself is the test of whether detection alone is enough.

## For Shopify / WP / BC / Magento windows

**Zero content changes to your arms.** Reinstall v1.11.33 optional; edition filter strips the HL section from your bundles.

**Shopify window specifically:** the ScriptTag sunset disclosure in this release's inventory concerns live Shopify stores you may hold client relationships for. Both sunset dates have now passed. The client-facing check is not a headless-arm matter and is not a warning; it is a "what did the client already lose" question. Owner assignment is the user's, not master's.

## For the user

Two asks embedded here you may want to act on independently:

1. **Confirm the fold landed** (Headless window's explicit ask) — this release notes document is the confirmation. If you want the one-line confirmation delivered another way, the tell is: does `webdesk-headless-v1.11.33.zip` contain both `templates/env-preflight.md` and `templates/discovery-audit.md`? Yes.
2. **The ScriptTag fact for Shopify client relationships** — Headless window's framing, accurate: this is a live-client-exposure item for whoever owns Shopify client relationships, not a headless-arm quality item. It sits at the sales/account boundary. This release records it in inventory; whoever owns those client relationships can run the check this week.

---

## Honest note

The drop happened because I did not run a "did the last delivery land" check between v1.11.30 and v1.11.32. The K4 focus that shaped v1.11.31 and v1.11.32 was legitimate work; the drop was still on me. The rule I would apply prospectively — check the arm's delivery ledger against the last bundle before shipping the next — is what the deferred tooling would automate. Whether the tooling gets built in the batch or the discipline gets applied manually in every release until then is a choice, and manual is the one I need to hold until the batch ships.

The Headless window's framing that this is the same failure class as K4, one level up, is exact. K4 said the arm verifies inputs but not outputs; the ship-gate verifies content-hash drift within the bundle stream but not delivery landing at the boundary. Both are input-verification tools with an output blind spot. The pattern is worth naming as a family, not two separate incidents.

The v1.11.32 release notes' claim that *"zero other-arm content touched, verified by directory diff before ship"* was true. It did not verify that everything that should have been touched **was** touched, which is exactly the shape of blind spot D-HL-SPEC-01 was written to prevent one level down.

---

Generated: 2026-08-27 by master (v1.11.33 — dropped-work recovery + ScriptTag disclosure + 4e reservation)
