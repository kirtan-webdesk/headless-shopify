# WebDesk AI Delivery System — v1.11.21

**Release date:** 2026-08-06
**Type:** PATCH — URGENT amendment to D-KB-FIDELITY-01 + 6 headless proposals reconciled + 4 corrected headless files folded + audit-signer question ruled
**Trigger:** Headless skill-dev window escalation 2026-08-06 (found while running the first D-KB-FIDELITY-01 sweep; rule as-written could revert ratified content).

---

## STOP — for Shopify / WP / BC windows

**Do NOT run D-KB-FIDELITY-01 sweeps against your arm until you reinstall v1.11.21.** v1.11.20's rule as-written was missing the precedence sentence. Running the sweep against the v1.11.20 rule would flag ratified content as defects (because your proposal files still say `status: PROPOSED`) and "fixing" them would revert your ratified content to superseded state.

v1.11.21 fixes this. Amendment below.

---

## Item 1 — D-KB-FIDELITY-01 amended (URGENT)

Added the missing precedence sentence:

> *The inventory is authoritative for **status** — what is ratified, closed, superseded, or open. The proposal is authoritative for **detail** — verbatim rules, numbered lists, check IDs, artifact names. When they disagree on status, the inventory wins on status; the proposal's detail is still authoritative for its numbered content.*

**Concrete example the Headless window hit:** their first sweep flagged as critical defects the arm's statements that:
- Architecture C is `SUPPORTED-ON-DEMAND` (ruled v1.11.17)
- BC GraphQL Storefront rate limits are 10,000 complexity / depth 16 (verified 2026-08-06)
- `@shopify/hydrogen-react` is usable (verified 2026-08-06)

All three are correct and ratified. All three look like violations against a proposal frozen before the ruling. **A window following D-KB-FIDELITY-01 literally would have reverted architecture D back to blocked** — the deal sales was already held on once. The rule was meant to stop the KB drifting AWAY from decisions; as originally written it could push the KB BACKWARDS to superseded ones.

Precedence rule now written explicitly in the D-KB-FIDELITY-01 entry (inventory HL section). Full text updated to distinguish status (inventory wins) from detail (proposal wins).

## Item 2 — 6 headless proposals reconciled in place

**Method: option 1 per Headless window's recommendation.** For each of D-HL-STACK-01 / TYPES-01 / DISCOVERY-01 / APPS-01 / SEC-01 / ENV-01 proposal files:

- **Frontmatter updated:** `status: PROPOSED` → `RATIFIED`; `ratified_by: master (PENDING)` → `master (v1.11.17)`.
- **"Master reconciliation" section added at the top of each**, listing every open question that has been ruled since ratification and every TODO-VERIFY that has closed. Follows the strike-through pattern D-HL-STACK-01 already used at its own OQ3.
- **Body detail unchanged** — still authoritative for verbatim rules, numbered lists, check IDs.

Verified in shipped `webdesk-headless-v1.11.21.zip`: all 6 proposals show `status: RATIFIED`.

## Item 3 — 4 corrected Headless arm files folded

The sweep found 6 real defects. All fixed by the Headless window and folded:

| # | Defect | File | Consequence had it shipped further |
|---|---|---|---|
| 1 | **Checkout Plus-gating asserted** as fact when open question is whether it's gated. Self-violating: sits in the file that defines `HEADLESS-HALLUCINATION-01`. | `09-forbidden.md` | Would guide sales toward Plus-gated pricing on a capability whose gating hasn't been verified. |
| 2 | **Verbatim GitHub test edited inside its own quote marks** — *"public on GitHub tomorrow"* → *"public tomorrow"*, *"other than"* → *"beyond"*. Same test was exact in `05-security-baseline.md`; arm contradicted itself. | `09-forbidden.md` | Different arms of the same test = different tokens developers grep for. Discipline breaks. |
| 3 | **Bucket-1 evidence bar weakened** from *"named in that [vendor's] documentation"* to *"we identified the specific endpoints"*. | `09-forbidden.md` | Moves the burden from vendor's docs to our own inspection — the precise inference the bar exists to forbid. |
| 4 | **C's "give-up" inventory replaced, not extended** — dropped cart handlers, analytics/consent components, Customer Account API client wiring. | `00-overview.md` | **This list IS the pricing basis for C engagements.** Without those three, a C estimate looks like a Hydrogen estimate. Underquotes C by the exact three items every C engagement builds. Restored. |
| 5 | **B's "highest-risk of the four" position absent entirely**, along with the "do not sell B as Hydrogen-just-on-Vercel" instruction. B's promotion to full support was commercial (named client), not a finding that the risk went away. | `00-overview.md` | Sales pitching B as low-risk because the arm called it "full support" without carrying the risk framing. Restored with the promotion-was-commercial distinction explicit. |
| 6 | **Unsourced vendor claim in a Tier-0 file:** *"Shopify rate-limits public Storefront tokens per-IP"* in `D-HL-SEC-01`. No register entry. Load-bearing for public-vs-private token trade. | `09-forbidden.md`, `05-security-baseline.md` | Marked unverified and not client-quotable pending verification, rather than deleted. Verification pending. |

Two of these (4 and 5) affect sales pricing directly — worth flagging to sales, not just to the arm.

`HEADLESS-HALLUCINATION-01` extended per D-KB-FIDELITY-01: two classes, facts and ratified decision content, with the "stricter than ratified is not the safe direction" reasoning written in.

**Headless arm SKILL.md: 0.4.0 → 0.5.0.**

## Item 4 — Audit-signer question RULED

D-HL-DISCOVERY-01's open question ("Who signs the audit — Internal PM, or client sign-off") ratified per Headless window's recommendation:

**Client sign-off on the dropped-functionality column only, not the whole audit.**

Rationale: the rest of the audit is internal working product full of vendor detail a client has no basis to approve; asking for a signature on it invites line-by-line negotiation of technical findings. The dropped-functionality column is the one section a client can meaningfully assess and the exact document that prevents the UAT dispute. Template change is minimal: one signature line on one section.

Recorded in canonical inventory HL section. The Headless window's KB still says the question is open pending master's ratification; their next content pass will fold in the ruling.

---

## Not fixed in this release (recorded so a future sweep doesn't mistake absence for drift)

Per Headless escalation §3:

- **D-HL-APPS-01's five qualification questions** exist nowhere in the arm. Belong in `knowledge/08-app-integrations/`, currently unwritten. On the build list.
- **D-HL-TYPES-01's five retainer change-order triggers** and cross-cutting rules 2, 3, 6, 7 and 8 belong in `projects/retainer/`. Unwritten. On the build list.

Both authoritative in the proposal files; not yet homed in arm KB. Sweep should treat these as "unwritten," not "drifted."

---

## Ship-gate results

| Bundle | Ship gate |
|--------|-----------|
| `webdesk-ai-delivery-system-v1.11.21.zip` (full) | PASS |
| `webdesk-shopify-v1.11.21.zip` | PASS |
| `webdesk-wp-elementor-v1.11.21.zip` | PASS |
| `webdesk-bigcommerce-v1.11.21.zip` | PASS |
| `webdesk-headless-v1.11.21.zip` | PASS |
| `webdesk-spine-only-v1.11.21.zip` | PASS |

Content-hash version-bump check: PASS. Headless content changed AND SKILL.md bumped 0.4.0 → 0.5.0 correctly. Proposals reconciliation (metadata frontmatter change + inserted reconciliation section) also changed content hash; that's expected.

Verified in shipped bundles:
- All 6 headless proposals show `status: RATIFIED` in `_decisions/proposals/headless/*-proposal.md`
- D-KB-FIDELITY-01 amendment ("PRECEDENCE RULE") present in shipped inventory
- Verbatim binding rule sentence 3 present in shipped `00-overview.md`, `09-forbidden.md`, `12-discovery-audit.md`

---

## For the Headless window

**Reinstall `webdesk-headless-v1.11.21.zip`.** All 4 corrected files folded; all 6 proposals reconciled to ratified state; D-KB-FIDELITY-01 precedence rule amended; audit-signer ruled per your recommendation.

Your KB still marks the audit-signer question as open per the boundary policy — next content pass, fold in the ruling.

**On sales visibility for defects 4 and 5 (C give-up inventory + B honest position):** worth telling sales directly, not waiting for the next release notes propagation. C engagements priced against the truncated give-up list = underquoted by the three missing items; B engagements pitched without the "highest-risk of the four" caveat = wrong risk framing to the client. Both flagged in the release notes but sales won't read release notes for this.

## For Shopify / WP / BC windows

- Reinstall v1.11.21 BEFORE running any D-KB-FIDELITY-01 sweep on your arm.
- Your proposals may still say `status: PROPOSED / ratified_by: master (PENDING)` — same class of thing the Headless window hit. The v1.11.21 precedence rule means these do NOT need immediate reconciliation for correctness (inventory wins on status), but reconciling in place (option 1 pattern demonstrated by the Headless proposals in this release) makes future sweeps unambiguous.
- Recommend: when you're ready to sweep, first reconcile your own arm's proposals to `status: RATIFIED` with a reconciliation section at top listing any OQs / TODO-VERIFYs the inventory has since closed. Then sweep your KB against the reconciled proposals.

---

## Honest notes

1. **The rule I ratified in v1.11.20 was unsafe.** The precedence question — inventory vs proposal for status — was answerable in the same session I wrote the rule. I didn't answer it. The Headless window hit the failure mode in their first real use.

2. **This is now three releases in a row where an arm window has caught something I shipped as broken.** v1.11.19 (proposals content drift), v1.11.20 (my "most complete surviving form" claim was false), v1.11.21 (the fidelity rule itself could revert ratified content). Each is a specific check I could have run before shipping and didn't. Pattern: I ship the rule, then the arm runs it against real material, then the rule shows its holes. Should be flipping that — dry-run the rule against real material before shipping.

3. **The "sweep against your own proposals" guidance I issued in v1.11.20's "For Shopify / WP / BC windows" section was actively dangerous under the v1.11.20 rule.** If any of those windows started that sweep before v1.11.21 lands, they'd be applying the same broken rule. STOP notice at top of these release notes because arms don't necessarily reinstall before every operation.

4. **Defects 4 and 5 from the sweep are pricing bugs, not just fidelity bugs.** C give-up inventory truncated = C engagements underquoted. B honest position absent = B pitched at wrong risk. Both were in KB content shipped since v1.11.19. If any commercial conversation on a C or B engagement has happened during those two weeks, worth reviewing.

---

Generated: 2026-08-06 by master (v1.11.21 — D-KB-FIDELITY-01 amended with precedence rule + 6 proposals reconciled + 4 corrected files folded + audit-signer ruled)
