# WebDesk AI Delivery System — v1.11.26

**Release date:** 2026-08-11
**Type:** MINOR — Headless Architecture B folded (delta over A) + correction to v1.11.25 release notes.
**Trigger:** Headless skill-dev window buildout packet 2026-08-06 + note-to-master correction.

---

## Correction to v1.11.25 release notes

**v1.11.25's release notes said "`@shopify/hydrogen` (13.0.4)".** Wrong on both the number and the package. **Hydrogen is `2026.4.4` (CalVer). CLI is `13.0.3` (SemVer). `13.0.4` isn't a version of either.**

The shipped v1.11.25 content was correct — spec, register, and live `npm view` all agree. Only my notes were wrong. Not a harmless typo per Headless window's flag: CalVer-vs-SemVer is the exact confusion this arm keeps correcting. It's why a Hydrogen upgrade is an API-version migration (engagement type 5), not a version bump. Handing Hydrogen a SemVer-shaped number in a doc other windows and sales read teaches the wrong model — and it landed in the paragraph praising verification-first authorship, which is the worst place for it.

Discipline note: even a passing paraphrase of a verified fact in a release note should get diffed against the register, same way KB content is. Adding to my ship checklist.

## What shipped

**Architecture B (`architectures/shopify-hydrogen-selfhost/00-delta-over-A.md`)** — genuine delta, not parallel copy. References A rather than restating it; content is only what changes.

Three flag points from the Headless window worth surfacing:

- **Runtime-family distinction.** B's host set spans worker runtimes and Node runtimes, and which one you're on changes the dependency-audit outcome. Self-hosting doesn't automatically escape Oxygen's constraints — on a worker-runtime host the same class of restriction applies. The audit runs against the actual target runtime, never against "self-hosted" as a category.
- **Caching trap.** `CacheShort` and `CacheLong` are still exported and still work, because they describe cache behaviour. What backs them on Oxygen doesn't come with you. Seeing the helpers still there is exactly what makes someone assume the infrastructure is too. B estimates go wrong here.
- **B3/B4 are why the deploy spike isn't a formality.** B3: Oxygen-specific code removal as an enumerated file list. B4: custom server context proven through `getLoadContext` — not done until a loader has read the storefront client out of that context on the deployed host.

**Arm SKILL.md: 0.10.0 → 0.11.0.**

## Ship-gate results

All 6 bundles PASS. Arm-scoped Headless validator: 17 files, 0/0.

## For the Headless window

Reinstall `webdesk-headless-v1.11.26.zip`. B folded as delta over A per your pattern. Next per your ordering: D (`bigcommerce-catalyst/`), then `projects/` — the five project-type skills plus the retainer intake where D-HL-TYPES-01's five change-order triggers and the 8-hour threshold finally get homed. C stays deferred on its commercial hold.

## For Shopify / WP / BC windows

Reinstall v1.11.26 for tooling consistency. No content changes to your arms.

---

## Honest notes

1. **CalVer/SemVer typo landed in the paragraph praising verification-first authorship.** Perfect placement to teach the wrong model. Ship checklist now includes: any package/version reference in release notes gets diffed against the register before publish.

2. **Uploaded-zip vs message mismatch.** Headless window sent the shipped v1.11.25 bundle first (which had no B content); reuploaded the correct architecture-B zip on ask. Not a system problem — worth flagging that arm windows sometimes have working-directory recovery quirks after reinstall, and "the release is the backup" mechanism sometimes fights with in-flight authoring.

---

Generated: 2026-08-11 by master (v1.11.26 — arch B delta fold + CalVer/SemVer typo correction)
