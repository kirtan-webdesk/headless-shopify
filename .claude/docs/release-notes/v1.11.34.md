# WebDesk AI Delivery System — v1.11.34

**Release date:** 2026-08-27
**Type:** PATCH — Headless K4 capability gaps (2 of 4 authored), publication-scoping source URL approved. Headless-only. Zero other-arm content.
**Trigger:** `headless-HL-CAP-partial.zip` from Headless window 2026-08-27 with the per-gap triage.

---

## What ratified

**HL-CAP triage adopted verbatim from Headless window.** "Before or after the pilot" was the wrong split; the four capability gaps are not the same kind of thing. Per-gap ratification:

- **HL-CAP-001 — Metaobject-driven section patterns — PRINCIPLED DEFERRAL.** The only implementation-shaped one of the four; writing it from theory is exactly the reconstruction-drift failure that cost three releases in v1.11.19-20. Gate (`HL-SPEC-003`) now stops the failure at G4 instead of at the client. The pilot teaches the pattern; master does not invent it. The pilot fails safely, which is the property that makes this deferral defensible.
- **HL-CAP-002 — Channel publication scoping — DEFERRED pending source verification.** The arm's own `HEADLESS-HALLUCINATION-01` rule is what holds it. **Source URL APPROVED this release:** `https://shopify.dev/docs/storefronts/headless/building-with-the-storefront-api/products-collections` (first-party Shopify docs; meets the verified-facts rule). Headless window can close as `verified-facts §17` in the next fold.
- **HL-CAP-003 — Navigation sourcing — AUTHORED** in `knowledge/06-data-layer-patterns.md` §8b. Three-source framework (platform menus / metaobjects / hardcoded); the load-bearing decider is not technical: *when marketing wants to add a menu item on a Friday, what happens?* Explicit `HL-SPEC-003` silent-shape cross-reference — same failure family, one level up.
- **HL-CAP-004 — Live-store safety — AUTHORED** in `knowledge/11-environment-preflight.md` as *check 7b*. Resolves the contradiction the arm was carrying: check 7 forbids staging pointed at production; Migrate-to-Headless often *must* read production. Both true — the risk is the credential, not the data. Read-scoped catalogue access legitimate; write-capable token in a dev environment is one seed script from a live incident. Shared-quota consequence (`HL-SEC-004`) named: a load test against the client's store spends the quota the live storefront depends on.

**Family framing:** HL-CAP is capability, HL-SPEC is conformance. HL-CAP teaches how to build the correct thing; HL-SPEC detects when the built thing does not match the spec. The two together close the K4 asymmetry. Detection alone is blocking, not teaching — that framing carried into the inventory as ratified.

## Arm version — 0.16.0 → 0.17.0

Still 0.x per D-ARM-VERSION-01. Two capability KBs authored, one gated on a first-party source (URL now approved), one deferred by principle to the pilot. 1.0.0 waits on next pilot not repeating.

## Ship-checklist per D-MASTER-SCOPE-01

Diff v1.11.33 → v1.11.34:

- `README.md` (version bump)
- `_decisions/decision-inventory.md` (HL-CAP-001..004 ratified per-gap with the triage embedded; source URL approval recorded)
- `skills/headless/SKILL.md` (0.16.0 → 0.17.0, decision_refs updated)
- `skills/headless/knowledge/06-data-layer-patterns.md` (§8b navigation sourcing + anti-patterns 19-21)
- `skills/headless/knowledge/11-environment-preflight.md` (check 7b live-store safety)

**Zero other-arm content touched.** Shopify, WP, BC, Magento arms byte-identical to v1.11.33. Verified by directory diff before ship.

## Ship-gate results

All 6 bundles PASS. Content-hash confirmed `headless — content changed, SKILL.md bumped 0.16.0 → 0.17.0.`

---

## For the Headless window

Reinstall `webdesk-headless-v1.11.34.zip`.

**Publication scoping URL approved.** `https://shopify.dev/docs/storefronts/headless/building-with-the-storefront-api/products-collections` is a first-party Shopify docs URL and meets the verified-facts rule. Close as `verified-facts §17` on your cadence. Urgency depends on the "build actually starting" answer — asked to the user in this release.

**Metaobject deferral defended in the inventory.** The reasoning ("writing implementation from theory is the reconstruction-drift failure that cost three releases") is verbatim into `decision-inventory.md` so nobody re-litigates it under pressure. The pilot is what closes it.

**Cross-reference to `HL-SPEC-003` in nav §8b is the clean pattern.** The K4 pilot lost navigation from changes aimed elsewhere; §8b names it as a shared surface with a conformance row re-walked every sprint exit, and cross-links to the code that enforces it. That is how HL-CAP and HL-SPEC compose — build guidance names the detection code that fails the build if the guidance is not followed.

## For Shopify / WP / BC / Magento windows

**Zero content changes to your arms.** Reinstall v1.11.34 optional; edition filter strips the HL section from your bundles.

## For any developer starting a headless build against v1.11.34

The most useful thing to put in front of them, per the Headless window's guidance:

- **`templates/spec-conformance-ledger.md`** at G1, before any code. It is brand new and completely untested — this is the first build to run it. Flag where it is awkward rather than working around it; that feedback is what turns the arm from 0.x into 1.0.

Two caveats to hand over up front, not discover at G4:

- **No Metaobject-driven section pattern is in the arm.** That is the exact thing the last pilot failed on. `HL-SPEC-003` will now stop a static homepage at the gate instead of at the client, which is genuinely better, but nothing in the arm shows how to build the dynamic one. If the build has a Metaobject requirement, the developer is on their own for the implementation and should expect the gate to hold them to it. Deliberate — the arm would rather they discover the right pattern on a real build than follow one master invented.
- **Publication scoping is unverified.** If products do not appear in the storefront, check whether they are published to the sales channel before debugging code. The arm cannot say this as guidance yet because the source has not been folded, but a developer losing a day to it is the worst version of the caution.

---

## Honest note

The Headless window's per-gap triage ("authorable vs. needs verification vs. needs a real build") is a better decomposition than the "before or after the pilot" split I offered. Adopting it verbatim rather than restating it is the right move — the framing survives contact with the pressure to close everything at once, which is where the reconstruction-drift failure comes from.

The URL approval is straightforward — it is a first-party Shopify docs URL and the arm's own rule accepts first-party sources. Whether to push the Headless window to close it *this week* depends on whether a build is starting; that is not master's fact to have. Escalated to the user in the next turn.

The composition pattern in nav §8b (guidance cross-links to detection code) is the shape HL-CAP and HL-SPEC should always take together across the four gaps. Worth carrying into the remaining two when they are authored.

---

Generated: 2026-08-27 by master (v1.11.34 — HL-CAP-003 + HL-CAP-004 authored, HL-CAP-002 source approved, HL-CAP-001 principled deferral)
