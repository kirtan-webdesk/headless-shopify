# WebDesk AI Delivery System — v1.11.28

**Release date:** 2026-08-11
**Type:** MINOR — Retainer skill folded (closes last "unwritten, not drifted" gap) + D-HL-ENV-01 check 1b ratified (cross-project Node resolution).
**Trigger:** Headless skill-dev window buildout packet 2026-08-11 (retainer + ENV-01 amendment escalation).

---

## What ratified — D-HL-ENV-01 check 1b (additive, no renumbering)

Master invited this in v1.11.27's honest notes. Headless window escalated with an additive proposal that keeps IDs stable per D-KB-FIDELITY-01. Ratified as-is:

> **Check 1b — cross-project resolution.** Where the same workstation, container image or CI runner serves more than one headless architecture, the resolved Node version satisfies **every** architecture it will build. Read `engines` from **each scaffolder actually invoked**, not from the framework package it depends on — the two can disagree, and on BigCommerce the scaffolder is the stricter one. Record the resolved version in the environment matrix (check 7). Where no single version satisfies all, per-project version management is the answer and it is **named at preflight**, not improvised.
>
> *Current state (illustrative, dated to `pointers/verified-facts.md` §12a and §13): Hydrogen CLI `^22 || ^24`, `@bigcommerce/create-catalyst` `^24.0.0` — Node 24 is the only version that satisfies both.*

**Why 1b and not a check 10 rewrite:** the gap is in what check 1 covers, not in what needs an additional check. Additive keeps every existing check ID stable — no downstream doc renumbers, no SOW references break. D-KB-FIDELITY-01 discipline held.

**The wording is written to outlive the numbers.** The italicised current-state line is dated and will go stale; the rule ("read engines from each scaffolder actually invoked, resolve across every architecture the machine serves") survives version churn. That's the correct shape for a rule that lives in a decision text.

**Ratified in:**
- Inventory D-HL-ENV-01 entry — full 1b text
- `_decisions/proposals/headless/D-HL-ENV-01-proposal.md` reconciliation section — records the amendment per the D-KB-FIDELITY-01 precedence pattern

**KB update deferred to Headless window's next content pass** per authorship boundary. Master ratifies decision text; arm window folds ratified content into KB. `knowledge/11-environment-preflight.md` still carries the pre-1b text until they fold — expected shape.

## What shipped

**`projects/retainer/SKILL.md`** — closes the last "unwritten, not drifted" gap master recorded in v1.11.21.

D-HL-TYPES-01 §6's material now has a home:
- 5 change-order triggers (verified 5/5 against ratified text)
- 6 coverage areas (verified 6/6)
- 8-hour per-ticket threshold
- No-SLA default

Headless window's three commercially load-bearing points, in the order they'll matter:

- **Trigger 5 holds the boundary.** A retainer with no numeric per-ticket threshold has no boundary, and the "what's in the retainer" argument then happens with the client monthly.
- **The onboarding audit is billable work, not goodwill.** Waiving it to win a retainer converts an unaudited codebase into an unbounded liability priced as a fixed monthly fee; defects arrive as tickets we absorb.
- **No response-time SLA is consistent, not stingy.** Offering an SLA on a storefront whose hosting, monitoring and incident response belong to the client means committing to a response time for failures we cannot see.

Also included: architecture-specific onboarding items that get missed on an inherited build — notably that on B, the deploy path has never been validated by us because B1 was a gate on someone else's original build.

**Arm SKILL.md: 0.12.0 → 0.13.0. Retainer SKILL.md: 0.1.0 (initial).**

## Ship-gate results

All 6 bundles PASS. Arm-scoped Headless validator: 19 files, 0/0. Classifier inversion (v1.11.25) is covering `projects/` exactly as predicted — no re-fix needed.

## For the Headless window

Reinstall `webdesk-headless-v1.11.28.zip`. Retainer folded. 1b ratified in inventory + proposal.

**Next content pass folds 1b into `knowledge/11-environment-preflight.md`** per the authorship boundary — master ratifies decision text, you fold into KB. No urgency; the inventory carries the ratified text now.

Remaining: five project-type skills under `projects/` (New Build, Replatform, Migrate-to-Headless, Redesign, Framework Upgrade). Last structural piece before the arm is functionally complete.

## For Shopify / WP / BC windows

Reinstall v1.11.28. No content changes to your arms.

**Cross-agency pattern worth noting from 1b:** if any of your arms have engagement-level decisions where the gate is currently written per-project but the failure mode lives at the workstation / CI level, the same shape applies. Additive clause, durable wording, current numbers illustrative. Escalate if you spot one.

---

## Honest notes

1. **The escalation demonstrated D-KB-FIDELITY-01's precedence rule working end-to-end.** Additive amendment, no renumbering, current-state numbers explicitly marked as dated/illustrative, ratified text in inventory (authoritative for status), body detail preserved (proposal authoritative for numbered content), reconciliation section carrying the delta. That's the pattern the whole rule was designed for.

2. **The retainer skill closes the last v1.11.21 "unwritten, not drifted" gap.** That gap has been carried explicitly through 7 releases with the register annotation "sweep should treat these as unwritten, not drifted." Now written. Worth remembering: recording something as "unwritten but tracked" is not a delay tactic — it prevents a future sweep from mistaking absence for defect. The pattern is worth reusing.

---

Generated: 2026-08-11 by master (v1.11.28 — retainer skill fold + D-HL-ENV-01 check 1b ratified)
