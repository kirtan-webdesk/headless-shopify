# WebDesk AI Delivery System — v1.11.25

**Release date:** 2026-08-06
**Type:** MINOR — Headless architecture A folded (verified-first authorship) + validator classifier inverted + register §12 records package-source verification.
**Trigger:** Headless skill-dev window buildout packet 2026-08-06 (arm A) + escalation on `architectures/` files falling outside the classifier.

---

## Sales-visible finding — cache defaults

**`CacheShort()` is 1 second fresh / 9 seconds stale.** Not minutes. `CacheLong()` is 1 hour fresh / 23 hours stale.

The Headless window pulled the published `@shopify/hydrogen` package and recorded exactly what it exports in `pointers/verified-facts.md` §12. Nearly every reader assumes "short" means minutes and sizes origin load against that guess. Nothing in the API name signals otherwise. `CacheLong` also means a price can be up to 23 hours stale — a commercial decision worth naming in an SOW, not a default to inherit.

Textbook HEADLESS-HALLUCINATION-01 candidate that the register-first authorship discipline caught before it shipped as an assertion.

---

## What shipped

**Architecture A KB (`architectures/shopify-hydrogen-oxygen/00-reference.md`):**
- Complete export/import surface for `@shopify/hydrogen` (13.0.4) and `@shopify/cli-hydrogen` (13.0.3), pulled from the packages themselves — not from docs. Automated check confirms every named Hydrogen export in the A spec traces to a register entry.
- Verified D-HL-ENV-01 check A3 (`hydrogen link` + `env pull` both exist) against the CLI command surface. Also surfaced `debug/cpu` as the actual profiling tool for the 400 ms startup and 30 s CPU ceilings.
- Register §12 records what came back from the packages, timestamped 2026-08-06.
- Four items marked as nameable-but-not-assertable rather than claimed. Notable: `setup/markets` CLI command exists, but the command existing says nothing about what any plan permits — Shopify Markets per-plan capability is still open item 3.

**Arm SKILL.md: 0.9.0 → 0.10.0.**

## Escalation closed — `architectures/` classifier gap

The Headless window flagged that `architectures/` files fell outside `validate-frontmatter.py`'s classifier. 15 of 16 arm files were being validated; the unvalidated one was the A spec. Same class as the v1.11.18 `pointers/` gap. Third recurrence (v1.11.3 knowledge/, v1.11.18 pointers/, v1.11.25 architectures/).

**Per their recommendation: inverted the default rather than adding one directory.**

Under `skills/{arm}/{subdir}/`, everything is KB unless explicitly template or docs. Catches architectures/, projects/ (unwritten, headed for the same hole), and any future contract-mandated arm subdirectory. Restricted to `skills/` trees; `tools/` and repo-root trees keep their existing "ignored" behavior.

`README.md` and `gates.md` are now classified as `docs` (human-facing readmes, not runtime-loaded KB). `_decisions/proposals/*.md` also classified as `docs` — they carry their own proposal frontmatter schema, not the KB schema.

## Validator debt exposed (pre-existing, not v1.11.25 regression)

The inversion exposed **12 spine `_spine/shared-knowledge/*.md` files** that ship without KB frontmatter (missing tier / load_when / description). These are load-bearing files per the tiered KB loading system and should have proper frontmatter. They just weren't being validated before because the classifier walked past them.

Not a v1.11.25 regression — same class of debt that v1.11.12 free-text scanner (18 files), v1.11.14 footer-version validator (7 files), v1.11.18 pointers/ classification (3 files) exposed on their first runs.

Filed as v1.11.26+ sweep. Ship gate is not affected (ship-bundle.sh doesn't invoke validator error count).

## Ship-gate results

All 6 bundles PASS.

**Validator counts:**
- Arm-scoped Headless: 16 files, 0 errors, 0 warnings.
- Repo-root: 249 files, 12 errors (spine debt, above), 116 warnings (cross-arm load_when debt from v1.11.18).

---

## For the Headless window

Reinstall `webdesk-headless-v1.11.25.zip`. Architecture A folded. Verification-first authorship discipline paid off — CacheShort finding alone justifies the register-before-assert pattern.

Classifier invert covers your future `projects/retainer/` content when you land it.

Next per your ordering: B as delta over A, then D.

## For Shopify / WP / BC windows

- Reinstall v1.11.25 for the classifier invert (protects your future `projects/` and `templates/` content from the same silent-load class of bug).
- Recommended: after reinstalling, run `python3 tools/scripts/validate-frontmatter.py skills/{your-arm}` — arm-scoped run to see what surfaces. New surfacings are the good kind: they're what would previously have loaded silently or not at all.

---

## Honest notes

1. **Third time a contract-mandated directory has fallen outside classify(). Inverting the default finally fixes the pattern.** Enumerating validated subdirectories was always going to keep breaking as arms added contract-mandated dirs (projects/ is next). The invert makes the classifier default-safe.
2. **My first inversion attempt caught `tools/` too.** 49 spurious errors. Restricted scope to skills/-prefixed paths with SKILL.md-at-walk-root detection for arm-scoped runs. Same pattern as the v1.11.16 locale bug and v1.11.18 arm-scoped glob bug: I ship a tool, arm runs it, I discover the assumption I baked in. Working on running these dry-runs before shipping.
3. **The v1.11.24 handoff note ("expect verification-heavy work for A") was right.** Headless window found real corrections in the packages themselves rather than trusting docs — CacheShort as 1s-not-minutes is a defect this arm would have shipped without the register-first discipline. HEADLESS-HALLUCINATION-01 (extended to decision content per D-KB-FIDELITY-01) is doing its job.

---

Generated: 2026-08-06 by master (v1.11.25 — arch A fold + classifier invert + CacheShort finding)
