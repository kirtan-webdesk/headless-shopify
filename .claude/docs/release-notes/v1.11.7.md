# WebDesk AI Delivery System — v1.11.7

**Release date:** 2026-07-02 (late evening)
**Type:** PATCH — WP New Build project skill folded in + 3 spine escalations addressed
**Trigger:** WordPress skill-dev window HANDOFF dated 2026-07-02 (New Build shipped)

---

## Why v1.11.7

WP skill-dev window delivered the second WP/WC project-type skill — **New Build** (v1.0.0, 15 files). Ships as-authored plus:
1. Two new decision codes ratified into canonical inventory
2. Spine cascade extended with project-type derivation (gap flagged in v1.11.6 audit, closed here)
3. ACF-07 load_when tag mismatch fixed (bundled here rather than deferred — cheap, related)

Zero content changes to Redesign skill, other WP KB files, Shopify, or master persona.

---

## New WP project skill shipped

`skills/wordpress-woocommerce/projects/new-build/` — 15 files, v1.0.0:

```
SKILL.md                                   (v1.0.0)
gates.md
knowledge/
  01-discovery-and-requirements.md         greenfield intake; commerce y/n
  02-information-architecture.md           full page + template inventory (D-WP-NB-02)
  03-builder-selection.md                  mandatory G0 builder question (D-WP-NB-01)
  04-design-system-setup.md                tokens + HTML mockups for ALL templates
  05-theme-and-content-architecture.md     theme scaffold; branches ACF/Elementor/Block
  06-woocommerce-setup.md                  greenfield store; INT-002 applies
  07-launch-and-seo-foundation.md          first go-live; SEO/indexing day-one
  08-new-build-risks.md                    greenfield risk register
templates/
  discovery-brief.md
  sitemap-page-inventory.csv
  builder-decision.md
  content-model.md
  launch-checklist.md
```

Design decisions preserved verbatim from WP window's HANDOFF:
- **WP/WC-native, NOT Shopify-mirrored.** Uses WP Redesign as structural template; content is WP/WC-native. If parallelism with `shopify/projects/new-build/` becomes desirable later, that's a follow-up alignment task.
- **Builder-neutral (single skill).** New Build branches at build phase (`05`) to ACF path or Elementor path via `platform_config.builder`. Not forked per builder.
- **v1.0.0 for new skill** (frontmatter and footer agree). Honest semver, not era-matched to arm version. Prevents mismatch class caught in Redesign footer earlier this cycle.

Validator on the folded canonical: 210 files, 0 errors, 1 acceptable warning.

---

## Decisions ratified

Under WP section, near D-WP-RD-01:

- **D-WP-NB-01** (2026-07-02, v1.11.7) — WP New Build builder selection is a mandatory G0 onboarding question (Classic+ACF / Elementor / Block editor). Recorded in `CLAUDE.md.platform_config.builder`; drives cascade tags. No design or scaffold work proceeds until set.
- **D-WP-NB-02** (2026-07-02, v1.11.7) — WP New Build: full information-architecture / page + template inventory approved at G1 BEFORE any page is built; used as G5 completeness checklist. No home-only partial builds.

Both codes are now defined, so `decision_refs: ["D-WP-NB-01", "D-WP-NB-02"]` in the New Build KB files resolve cleanly.

---

## Spine fixes shipped

### Cascade project-type derivation (E2 from WP HANDOFF — gap flagged in v1.11.6 audit)

`_spine/orchestrator/knowledge/06-agent-cascade.md` — added to derivation rules:

```
IF CLAUDE.md.platform_config.project_type is set:
  ADDITIONALLY ACTIVATE:
    - project-<project_type>
      Example: project_type = new-build → activate project-new-build
                project_type = redesign → activate project-redesign
                (etc — full list in file)
```

This ensures the New Build skill's `load_when: ["wordpress-new-build-active", "project-new-build", "platform-wordpress"]` actually intersects the emitted tag set. Same rule applies to every future project-type skill in every platform arm.

Platform arms may still register additional `<platform>-<project-type>-active` tags in their arm's `00-overview.md` "Cascade tag registration" section.

### ACF-07 load_when tag mismatch (E3 from WP HANDOFF — pulled forward from v1.12.0)

`wordpress-woocommerce/knowledge/07-classic-editor-and-acf.md` — expanded `load_when`:

- Before: `["wordpress-classic-acf-build"]` — did NOT match the orchestrator's emitted `acf-classic-build` tag
- After: `["wordpress-classic-acf-build", "acf-classic-build", "wordpress-platform-active", "code-production"]`

Backward-compat: the old `wordpress-classic-acf-build` tag stays for anything already using it. New canonical: `acf-classic-build`. Any WP session that emits either tag will load the file.

Note: this doesn't preempt the v1.12.0 ACF Architecture content adoption from the friend's skill. The 07 file's CONTENT stays as-is; only its trigger tags expanded. When v1.12.0 does the ACF Architecture content refresh, that's a separate pass on the file.

---

## Version state

| Component | Was | Now |
|-----------|-----|-----|
| System | v1.11.6 | **v1.11.7** |
| `wordpress-woocommerce/SKILL.md` | v1.12.1 | **v1.12.2** (registers New Build) |
| `wordpress-woocommerce/projects/new-build/SKILL.md` | (new) | **v1.0.0** |
| `wordpress-woocommerce/projects/redesign/SKILL.md` | v1.10.0 | v1.10.0 (unchanged) |
| Other platform skills | unchanged | unchanged |

---

## Ship-gate integrity

All 4 bundles built through `ship-bundle.sh` (D-SHIP-GATE-01 enforced):

- `webdesk-ai-delivery-system-v1.11.7.zip` — full canonical, integrity PASS
- `webdesk-spine-only-v1.11.7.zip` — master window, integrity PASS
- `webdesk-shopify-v1.11.7.zip` — Shopify edition, integrity PASS (spine contamination scan clean)
- `webdesk-wp-elementor-v1.11.7.zip` — WP edition, integrity PASS (spine contamination scan clean; includes the new New Build skill)

Zero bundles shipped past a red gate.

---

## What the WP window sees on reinstall

- New Build skill files under `projects/new-build/` — same as their dev bundle
- Platform SKILL.md at v1.12.2 with New Build registered — same
- Their proposed D-WP-NB-01 / D-WP-NB-02 codes now defined in inventory
- Spine cascade file updated to emit `project-<type>` tags automatically
- `07-classic-editor-and-acf.md` expanded to eager-load on the correct emitted tag
- Everything else unchanged from v1.11.6

WP window should verify: fresh session on a new-build WP+WC+Elementor project → cascade emits `project-new-build`, `elementor-build`, `wordpress-platform-active`, and the New Build SKILL.md loads eagerly. If it doesn't load, that's a NEW escalation.

---

## What's still deferred

Per the WP HANDOFF §Suggested next: remaining WP project-type skills — Migration, Version Upgrade, B2B/Wholesale, Multi-region/Multi-store. No priority forced. When the WP window picks the next one, the New Build pattern (WP/WC-native, single skill with builder branches, v1.0.0 honest-semver, decision codes proposed via HANDOFF) is now the reference model.

Also deferred (per v1.11.6 policy):
- v1.12.0 ACF Architecture content adoption from friend's skill
- Cross-cutting skills buildout
- QA Agent 9-step protocol rewrite

---

## Honest notes

1. **This makes 5 patches in 32 hours** (v1.11.3 through v1.11.7). I flagged "slowing down" after v1.11.6. Then the WP New Build arrived within 3 hours. Not going to refuse a clean HANDOFF because I said I'd slow down — but the pattern is worth watching. If v1.11.8 arrives before v1.11.7 has been in the field for at least a work week, we're churning.

2. **Every escalation this cycle came pre-analyzed by the platform window.** WP's HANDOFF diagnosed the cascade project-type gap in the v1.11.6 audit AND flagged ACF-07 as a related pull-forward. Master's job was ratification + folding + shipping. Cost of master's time: ~15 minutes actual work.

3. **The ship-gate is doing its job silently now** — no forced iteration this cycle because I remembered to genericize as I went. First patch in the sequence where I didn't have to re-ship a bundle mid-cycle.

4. **The New Build skill has NOT been piloted.** v1.0.0 is authorship, not validated content. First pilot will surface things the WP window couldn't predict. Standard K4 feedback loop applies.

---

Generated: 2026-07-02 by master (v1.11.7 patch — WP New Build skill ratified)
